# 🗓️ Planned Tasks: 2026-04-13 (Post-Session)

## 1. 개요 (Overview)
오늘 수집된 섀도우 모드 데이터를 기반으로 진행할 핵심 유지보수 및 리팩토링 항목입니다.

---

## 2. 예정 작업 내역 (Target Tasks)

### 2.1 [L3] 긴급: 포지션 복구 로직 강화 (오디텍 버그 수정)
- **현상**: 앱 재시작 동기화 시 데이터 로딩 지연으로 인해 `entry_bar_index`가 0으로 초기화되며 accidental Time Exit(시간 초과 청산) 발생.
- **해결 방안**:
    - `sync_position` 시 `data` 부재 시 `bars_held` 계산 유예 또는 현재 봉 기준 초기화 적용.
    - `entry_time` 기반의 정밀 보유 봉 수 추정 로직 도입.
- **예상 등급**: L3 (핵심 로직 수정)

### 2.2 [L4] 전략 엔진 고도화: Modular GuardEngine 리팩토링
- **목표**: `strategy.py`에 산재된 가드 로직을 `guards/` 폴더 내 모듈형 클래스로 분리.
- **세부 내용**:
    - `RiskGuard`, `RewardGuard`, `RegimeGuard`, `MTFGuard` 독립 모듈화.
    - 각 가드별 개별 ON/OFF 및 상세 섀도우 모드 제어 스위치 구현.
    - 오늘 수집된 `shadow_guard_log.jsonl` 분석 결과를 바탕으로 가드 임계치(Threshold) 최적화.
- **예상 등급**: L4 (아키텍처 변경)

---

## 3. 데이터 검토 일정 (Data Review Schedule)
- **장 종료 후 (~16:00)**: `logs/shadow_guard_log.jsonl` 파일 추출 및 요약 보고서 작성.
- **분석 세션**: 수집된 가드 히트 내역 중 '유효한 방어'와 '불필요한 차단'을 구분하여 가드 엔진 설계에 반영.

---
*로그 작성자: Antigravity AI*
